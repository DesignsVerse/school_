export interface Feature {
  id: string | number;
  slug: string;  // Add slug field
  category: string;
  categoryColor: string;
  location: string;
  time: string;
  title: string;
  description: string;
  price: string;
  image: string;
}